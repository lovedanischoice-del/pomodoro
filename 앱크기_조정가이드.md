# 📐 앱 크기 조정 가이드

## 🎯 조정 가능한 CSS 속성

### 파일 위치
`e:\바이브코딩\pomodoro\style.css`

---

## 1️⃣ 앱 전체 너비 조정

### 현재 설정
```css
/* 메인 앱 컨테이너 */
#app {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 480px;  /* ← 이 값을 조정하세요! */
}
```

### 조정 방법
**라인 번호**: 약 115-120줄

**옵션**:
- **더 작게**: `max-width: 400px;` (모바일 느낌)
- **기본**: `max-width: 480px;` (현재 설정)
- **더 크게**: `max-width: 600px;` (태블릿 느낌)
- **훨씬 크게**: `max-width: 800px;` (데스크톱 느낌)

---

## 2️⃣ 카드 내부 여백 조정

### 현재 설정
```css
/* 카드 스타일 */
.content {
    background: var(--bg-card);
    border-radius: var(--border-radius);
    padding: 40px 30px;  /* ← 이 값을 조정하세요! */
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
}
```

### 조정 방법
**라인 번호**: 약 122-127줄

**형식**: `padding: [위아래] [좌우];`

**옵션**:
- **더 좁게**: `padding: 30px 20px;` (공간 절약)
- **기본**: `padding: 40px 30px;` (현재 설정)
- **더 넓게**: `padding: 50px 40px;` (여유롭게)

---

## 3️⃣ 전체 페이지 여백 조정

### 현재 설정
```css
body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bg-primary);
    color: var(--text-primary);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;  /* ← 이 값을 조정하세요! */
    overflow-x: hidden;
}
```

### 조정 방법
**라인 번호**: 약 51-61줄

**옵션**:
- **여백 없음**: `padding: 0;` (화면 꽉 채움)
- **작은 여백**: `padding: 10px;`
- **기본**: `padding: 20px;` (현재 설정)
- **큰 여백**: `padding: 40px;`

---

## 4️⃣ 카드 모서리 둥글기 조정

### 현재 설정
```css
:root {
    /* 기타 */
    --border-radius: 24px;  /* ← 이 값을 조정하세요! */
}
```

### 조정 방법
**라인 번호**: 약 9-42줄 (CSS 변수 섹션)

**옵션**:
- **각진 느낌**: `--border-radius: 12px;`
- **기본**: `--border-radius: 24px;` (현재 설정)
- **매우 둥글게**: `--border-radius: 32px;`

---

## 🎨 추천 조합

### 조합 1: 작고 아담하게
```css
#app {
    max-width: 400px;
}
.content {
    padding: 30px 20px;
}
body {
    padding: 20px;
}
```

### 조합 2: 중간 크기 (현재)
```css
#app {
    max-width: 480px;
}
.content {
    padding: 40px 30px;
}
body {
    padding: 20px;
}
```

### 조합 3: 크고 여유롭게
```css
#app {
    max-width: 600px;
}
.content {
    padding: 50px 40px;
}
body {
    padding: 30px;
}
```

### 조합 4: 데스크톱 느낌
```css
#app {
    max-width: 800px;
}
.content {
    padding: 60px 50px;
}
body {
    padding: 40px;
}
```

---

## 📝 수정 방법

### 1단계: style.css 파일 열기
VS Code에서 `e:\바이브코딩\pomodoro\style.css` 파일을 엽니다.

### 2단계: 원하는 값 찾기
- **Ctrl + F** 눌러서 검색
- `max-width: 480px` 검색
- `padding: 40px 30px` 검색

### 3단계: 값 변경
원하는 크기로 숫자를 변경합니다.

### 4단계: 저장 및 확인
- **Ctrl + S** 저장
- 브라우저에서 **Ctrl + Shift + R** 새로고침

---

## 🔍 빠른 찾기

### VS Code에서 검색어:
1. `#app` → 앱 전체 너비
2. `.content` → 카드 내부 여백
3. `body {` → 페이지 여백
4. `--border-radius` → 모서리 둥글기

---

## 💡 팁

### 실시간 미리보기
브라우저 개발자 도구(F12)에서:
1. **Elements** 탭
2. `#app` 요소 찾기
3. 오른쪽 **Styles** 패널에서 값 직접 수정
4. 마음에 들면 CSS 파일에 적용

### 반응형 고려
모바일에서도 확인하려면:
1. **F12** → **Toggle device toolbar** (Ctrl + Shift + M)
2. 다양한 화면 크기에서 테스트

---

**어떤 크기로 조정하고 싶으신가요?**  
원하는 크기를 알려주시면 정확한 값을 제안해 드리겠습니다!
