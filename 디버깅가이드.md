# 🔍 디버깅 가이드

## 브라우저에서 확인해야 할 사항

### 1단계: 개발자 도구 열기
브라우저에서 `F12` 키를 누르세요.

### 2단계: Console 탭 확인
다음 메시지들이 나와야 정상입니다:
```
Firebase initialized successfully
Initializing navigation with 4 items
Navigation initialized successfully
```

**만약 에러가 있다면**, 빨간색으로 표시됩니다. 그 내용을 알려주세요.

### 3단계: Elements 탭에서 확인
1. Elements 탭 클릭
2. `Ctrl + F`로 검색창 열기
3. `nav-item` 검색
4. 4개의 버튼이 보여야 합니다:
   - `<button class="nav-item active" data-view="timerView">`
   - `<button class="nav-item" data-view="tasksView">`
   - `<button class="nav-item" data-view="statsView">`
   - `<button class="nav-item" data-view="settingsView">`

### 4단계: 네비게이션 수동 테스트
Console 탭에서 다음 명령어를 입력하세요:

```javascript
// 네비게이션 아이템 확인
document.querySelectorAll('.nav-item').length
```
**결과**: `4`가 나와야 합니다.

```javascript
// Settings 버튼 확인
document.querySelector('[data-view="settingsView"]')
```
**결과**: `<button class="nav-item" data-view="settingsView">...</button>`가 나와야 합니다.

```javascript
// 수동으로 Settings 화면 열기
const settingsBtn = document.querySelector('[data-view="settingsView"]');
settingsBtn.click();
```
**결과**: Settings 화면으로 전환되어야 합니다.

### 5단계: 네비게이션 함수 확인
```javascript
// initNavigation 함수가 실행되었는지 확인
typeof initNavigation
```
**결과**: `"function"`이 나와야 합니다.

```javascript
// 수동으로 네비게이션 초기화
initNavigation();
```
**결과**: Console에 "Navigation initialized successfully" 메시지가 나와야 합니다.

---

## 🚨 자주 발생하는 문제

### 문제 1: "initNavigation is not defined"
**원인**: `stats.js` 파일이 로드되지 않음  
**해결**: 
1. Network 탭에서 `stats.js` 확인
2. 빨간색이면 파일 경로 문제
3. `Ctrl + Shift + R` 강력 새로고침

### 문제 2: "Cannot read property 'addEventListener' of null"
**원인**: HTML 요소를 찾지 못함  
**해결**:
```javascript
// 앱이 보이는지 확인
document.getElementById('app').classList.contains('app-hidden')
```
`false`가 나와야 합니다. `true`면 온보딩 화면에서 멈춘 것입니다.

### 문제 3: 클릭해도 반응 없음
**원인**: 이벤트 리스너가 등록되지 않음  
**해결**:
```javascript
// 이벤트 리스너 확인
const btn = document.querySelector('[data-view="settingsView"]');
getEventListeners(btn)
```
`click` 이벤트가 있어야 합니다.

---

## 📝 보고해 주세요

다음 정보를 알려주시면 정확히 해결할 수 있습니다:

1. **Console 탭의 에러 메시지** (빨간색 텍스트)
2. **Network 탭에서 빨간색으로 표시된 파일** (있다면)
3. 위의 테스트 명령어 결과

---

**지금 브라우저에서 F12를 누르고 위 단계를 따라해 주세요!**
