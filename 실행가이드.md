# 🚀 Bbangmodoro 앱 실행 가이드

## ✅ 복구 완료!

모든 JavaScript 파일을 UTF-8 인코딩으로 새로 생성했습니다.

### 생성된 파일 목록
- ✅ `firebase-config.js` - Firebase 인증 및 클라우드 동기화
- ✅ `main.js` - 타이머 로직 및 할일 관리
- ✅ `stats.js` - **통계 + 네비게이션 로직 (탭 전환 핵심!)**
- ✅ `tasks-settings.js` - 할일 목록 및 설정 화면

---

## 🎯 지금 바로 실행하세요!

### 1단계: 서버 시작

PowerShell 또는 CMD를 열고:

```bash
cd e:\바이브코딩\pomodoro
python -m http.server 8000
```

**서버가 실행되면 이렇게 나옵니다:**
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

### 2단계: 브라우저 열기

1. **Chrome 브라우저** 실행
2. 주소창에 입력: `http://localhost:8000`
3. **반드시 강력 새로고침**: `Ctrl + Shift + R` (또는 `Shift + F5`)

### 3단계: 캐시 완전 삭제 (중요!)

브라우저가 오래된 파일을 불러오는 것을 방지하기 위해:

1. **F12** 눌러서 개발자 도구 열기
2. **Application** 탭 클릭
3. 왼쪽 메뉴에서 **Storage** 찾기
4. **Clear site data** 버튼 클릭
5. 브라우저 **완전 종료** 후 다시 열기

### 4단계: 다시 접속

```
http://localhost:8000
```

이제 **하단 탭(Timer, Tasks, Stats, Settings)**을 클릭하면 화면이 정상적으로 전환됩니다!

---

## 🔍 작동 확인 방법

### ✅ 네비게이션 테스트
1. 하단의 **Settings** 탭 클릭 → 설정 화면으로 전환
2. **Tasks** 탭 클릭 → 할일 목록 화면으로 전환
3. **Stats** 탭 클릭 → 통계 화면으로 전환
4. **Timer** 탭 클릭 → 타이머 화면으로 전환

### ✅ Google 로그인 테스트
1. **Settings** 탭으로 이동
2. 아래로 스크롤하여 **"Cloud Sync"** 섹션 찾기
3. **"Login with Google"** 버튼 클릭
4. Google 로그인 팝업 창이 뜨면 성공!

---

## 🐛 문제 해결

### 문제 1: "연결할 수 없음" 오류
**원인**: 서버가 실행되지 않음  
**해결**: 
```bash
cd e:\바이브코딩\pomodoro
python -m http.server 8000
```

### 문제 2: 탭 클릭해도 화면이 안 바뀜
**원인**: 브라우저 캐시에 오래된 파일이 남아있음  
**해결**:
1. `Ctrl + Shift + R` (강력 새로고침)
2. 안 되면 → F12 → Application → Clear site data
3. 브라우저 완전 종료 후 재시작

### 문제 3: Google 로그인 버튼이 안 보임
**원인**: Settings 화면으로 전환이 안 됨  
**해결**: 
1. F12 → Console 탭 확인
2. 에러 메시지가 있으면 알려주세요
3. `Ctrl + Shift + R` 다시 시도

### 문제 4: JavaScript 오류 발생
**원인**: 파일이 제대로 로드되지 않음  
**해결**:
1. F12 → Network 탭
2. 페이지 새로고침
3. 빨간색으로 표시된 파일 확인
4. 해당 파일명 알려주세요

---

## 📂 파일 구조 (최종)

```
e:\바이브코딩\pomodoro\
├── index.html              ✅ 메인 HTML
├── style.css               ✅ 메인 스타일
├── onboarding.css          ✅ 온보딩 스타일
├── stats.css               ✅ 통계 스타일
├── main.js                 ✅ 타이머 로직 (새로 생성)
├── stats.js                ✅ 통계 + 네비게이션 (새로 생성)
├── tasks-settings.js       ✅ 할일 + 설정 (새로 생성)
├── firebase-config.js      ✅ Firebase 설정 (새로 생성)
├── service-worker.js       ✅ PWA 서비스 워커
├── manifest.json           ✅ PWA 매니페스트
├── icon-192.png            ✅ 앱 아이콘
├── icon-512.png            ✅ 앱 아이콘
└── fire.mp3                ✅ 배경음악
```

---

## 🎉 성공 확인

다음이 모두 작동하면 성공입니다:

- ✅ 하단 탭 클릭 시 화면 전환
- ✅ Settings 탭에서 Google 로그인 버튼 보임
- ✅ Google 로그인 버튼 클릭 시 팝업 창 뜸
- ✅ Timer 화면에서 START 버튼 작동
- ✅ Tasks 화면에서 할일 추가 가능

---

## 💡 추가 팁

### 개발자 도구 활용
F12를 누르고 **Console** 탭을 보면 다음과 같은 메시지가 나와야 정상입니다:

```
Firebase initialized successfully
Initializing navigation with 4 items
Navigation initialized successfully
```

### 더미 데이터 생성 (통계 테스트용)
F12 → Console에서 다음 명령어 입력:

```javascript
generateDummyData()
```

그러면 통계 화면에 샘플 데이터가 표시됩니다!

---

## 📞 문제가 계속되면?

1. **F12 → Console** 탭의 에러 메시지 캡처
2. **F12 → Network** 탭에서 빨간색 파일 확인
3. 위 정보를 알려주시면 즉시 해결해드리겠습니다!

---

**지금 바로 시작하세요! 🚀**
