{
  "name": "bbangmodoro",
  "version": "1.0.0",
  "description": "장작 타는 소리와 함께하는 몰입형 포모도로 타이머",
  "main": "main.js",
  "scripts": {
    "build": "vite build",  
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npx cap run android",
    "copy": "npx cap copy",
    "update": "npx cap update",
    "deploy": "npx wrangler pages deploy ./www"
  },
  "keywords": [
    "pomodoro",
    "timer",
    "productivity"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.0.1",
    "@capacitor/cli": "^8.0.1",
    "@capacitor/core": "^8.0.1"
  }
}